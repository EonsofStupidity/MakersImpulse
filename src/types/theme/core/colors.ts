export interface TextColors {
  primary: string;
  secondary: string;
  link: string;
  heading: string;
}

export interface NeonColors {
  cyan: string;
  pink: string;
  purple: string;
}

export interface ThemeColors {
  primary: string;
  secondary: string;
  accent: string;
  text: TextColors;
  neon: NeonColors;
}